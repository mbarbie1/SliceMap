# A single .jar file can contain multiple plugins, specified in separate lines.
#
# The format is: <menu>, "<menu label>", <class name>
#
# If something like ("<arg>") is appended to the class name, the setup() method
# will get that as arg parameter; otherwise arg is simply the empty string.

Plugins>SliceMap, "SliceMap", main.be.ua.mbarbier.slicemap.SliceMap_
Plugins>SliceMap, "Comparing annotations", main.be.ua.mbarbier.slicemap.Compare_Annotation
Plugins>SliceMap, "Manual Annotation and Curation", main.be.ua.mbarbier.slicemap.Manual_Annotation_Curation
Plugins>SliceMap, "Region analysis", main.be.ua.mbarbier.slicemap.Analysis_Regions
